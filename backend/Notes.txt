Connecting to MongoDB:
1)create a project in MongoDB with a cluster
2)copy the url conection and put it inside a variable in the .env file
3)inside the config folder create a file named db.js which will hadle the function that connects to MongoDB
4)add console logs with dotenv and call the function created in db.js to make sure it is working


Clerk Implementation And Notes:
1) create an application in Clerk
2) copy the .env and paste it in vscode (publishable key) if I am setting up an admin pannel
3) npm install @clerk/express
4) copy publishable and secret key from quick start (choose express)
5) import the clerk middleware in the server.js and put app.use(clerkMiddleware());
6)use clerk provider in the frontend if needed in the main.jsx
7)I will use clerk with inngest which will be as a webhook that will work to save the user in the mongodb



Inngest with clerk:
1)inside clerk connect to inngest then click on the create button in Clerk
2)in clerk click on edit and choose the user created edited deleted
3)in backend folder install npm i inngest


Cloudinary:
1)create a project and find the cloud name and public and secrete credentials
2)put the 3 variables in the .env
3)create a file named cloudinary.js in the config folder and initiate it

